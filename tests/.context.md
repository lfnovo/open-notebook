<context path="tests" updated="2026-04-12">

Pytest test suite for the Python backend. Covers domain models, API endpoints, utility functions, LangGraph workflows, and embedding/chunking logic.

## Summary

- conftest.py — shared pytest fixtures (test DB client, sample data, mock AI responses)
- test_domain.py — unit tests for domain model validation and repository functions
- test_models_api.py — integration tests for AI model API endpoints
- test_notes_api.py — integration tests for notes CRUD endpoints
- test_graphs.py — integration tests for LangGraph workflow execution
- test_chunking.py — unit tests for text chunking strategies
- test_embedding.py — unit tests for embedding generation helpers
- test_utils.py — unit tests for miscellaneous utilities
- test_url_validation.py — unit tests for URL sanitization/validation
- test_podcast_path.py — unit tests for podcast file path handling

## Guidance

- SHOULD run tests with `uv run pytest tests/` before committing backend changes.

</context>
